# Database Configuration
# For local development with Docker
DATABASE_URL=postgresql://discogsdash:discogsdash_password@localhost:5432/discogsdash

# Discogs API Configuration (OAuth)
# 1. Go to https://www.discogs.com/settings/developers
# 2. Click "Create an App"
# 3. Fill in the form (use any valid URL for callback - it won't be used)
# 4. Copy the Consumer Key and Consumer Secret below
DISCOGS_CONSUMER_KEY=your_consumer_key_here
DISCOGS_CONSUMER_SECRET=your_consumer_secret_here
DISCOGS_USERNAME=your_discogs_username

# Sync Configuration
# Cron schedule for automatic syncing (optional, defaults to daily at midnight)
SYNC_CRON_SCHEDULE=0 0 * * *

# Environment
NODE_ENV=production